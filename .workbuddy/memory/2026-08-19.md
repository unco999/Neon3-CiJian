# 2026-08-19 工作日志

## World UI 统一渲染 + 泛型组件：调研与施工文档

- 产出详细施工文档：`plan/neon3-world-ui-unified.md`（供其他 AI 施工）。

### 关键调研结论（底座大部分已存在，主要是「升级为正式路径 + 补缺口」）
- 深度测试 world UI quad 管线已存在：`crates/neon-wgpu-runtime/src/world_ui_pipeline.rs`
  （`WorldUiQuad{model,color}`、`MAX_WORLD_UI_QUADS=256`、Depth32Float + depth_write + Less、
  已有深度遮挡测试），目前仅作 lab 模式。
- 视锥/近远裁剪投影已存在：`project_world_point_to_screen*` / `project_world_anchor_to_screen`
  （相机后/near/far/视锥外返回 None）。
- 批量 per-instance 输入机制已存在：`UiTemplateDeclaration{max_instances,row_schema,instance_key_field}`
  + `UiRepeatFrame`/`UiRepeatRow{stable_row_key,values}` —— 1000 实例 = 一个 template + 1000 row，无需新协议。
- 交互底座已存在：ID target(r32uint) + hit readback + `HostUiPointerClick` + `ui.host.inbound` + 语义事件。

### 要补的三个缺口
1. 泛型组件 `NeonWorldUi<V: NuiFlowVars>`（vars 字段即变量字段）+ trait 加 `apply_changes`（UI→Bevy 回写方向）。
2. world UI + screen UI 合并为一张带深度的统一纹理导出（screen UI 置顶，world UI 之间正确遮挡；
   与 3D 几何互遮挡依赖宿主用 Neon depth 合成）。
3. 交互闭环：Bevy 固定系统采样 ID target → 语义事件 → 变量变化 → 权威回流 `apply_changes` 回写组件字段。

### 用户设计要点（需求原文提炼）
- 场景 1000 个 world UI，每个来自不同 world position，绑一个 `.nui` + 一组变量。
- `NeonWorldUi` 是泛型组件，绑定变量是实现了 `NuiFlowVars` 的结构体，其字段即变量字段。
- 不在摄像机的 UI 完全剔除。
- world UI 与 screen UI 不能再分两块，最终渲染一张有正确深度关系的纹理给外部引擎。
- 鼠标交互走交互层纹理（ID 拾取），点击改变量后自动回写 Bevy。

## 施工进度（阶段 0 完成）

### 已实现并测试通过（bevy-nui-host/src/lib.rs，5 tests 全过）
- `NeonWorldUi<V: NuiFlowVars>` 泛型组件：`{ flow, vars, identity, anchor, offset, sent, visible }` + `stable_row_key()`。
- `NuiFlowVars` trait 扩展：新增 `apply_changes(&mut self, changes) -> Result<(), NuiFlowVarError>`
  （UI→Bevy 权威回写）+ `row_snapshot(&self, anchor) -> UiRepeatRow`（批量输入的全量行映射）。
- `NuiFlowValue` trait 增加反向 `from_ui_input_value`；新增 `NuiFlowVarError { UnknownKey, TypeMismatch }`。
- `nui_flow_vars!` 宏生成 `apply_changes`（按 key 匹配字段 + 类型校验）与 `row_snapshot`（全字段 BTreeMap）。
- 测试：`apply_changes_writes_back_typed_variables`、`row_snapshot_maps_a_full_template_row`。

### 施工时踩到的现实（重要，后续 AI 需知）
1. 代码已大幅演进（另有并发工作）：`neon-wgpu-runtime` 新增 `spawn_headless_external_server`（宿主进程内嵌 wgpu-runtime）；main.rs 已用它 + `spawn_forwarder` 双内嵌；Bevy 场景换成 Tokyo+Mei。
2. flow 语言目前是 `world panel ... anchor xxx`（单锚点），**还没有 `template` 声明**；`UiTemplateDeclaration` 未接入 flow 语法 → 1000 实例模板化要先扩 flow 语言。
3. `UiRepeatStore` 是**孤立**的：已定义+测试，但未接入 `UiRuntime` 结构体、无 RPC 暴露、无渲染消费者。
4. `UiRepeatStore::apply` 要求每行**完整**字段（非 sparse），与方案里"sparse row"有出入，需二选一。
5. 设计文档 `plan/neon3-world-ui-unified.md` 也被并发更新（新增三缓冲 D3D12 interop 契约、world panel 渲染坑、Tokyo fixture 验收等），施工前应先重读。


## 阶段 1（ui-runtime 半段）已完成（08-19 上午）
- 把 `UiRepeatStore` 接入 `UiHostAdapter`（新增 `repeats` 字段 + `apply_repeat`/`repeat_frame`）。
- 新增 RPC `ui.input.repeat`：`UiRuntime::handle_repeat_input` → 反序列化 `UiRepeatFrame` → `adapter.apply_repeat`，返回 `{accepted_rows, overflow_rows, diagnostics}`。
- 路由 + capability `ui.input.repeat.v1` 已登记；`UiRepeatApplyResult` 加了 Serialize/Deserialize。
- 新增测试 `repeat_input_rpc_applies_batched_instances_through_host_adapter`（91 tests 全过）。
- 修正认知：flow 语言其实**已支持** `template` 声明（`template X capacity N key K overflow_summary`），但 `row_schema` 被硬编码为 `{"row_key": U32}` 单字段；world UI 的多类型变量 schema 仍需扩 flow 语法。

## 三缓冲 D3D12 interop 死锁修复（08-19，用户报"坐标更新但纹理不重绘"）
- 根因：producer `min_by_key(frame_sequence)` 挑最旧 buffer + GPU `Wait(consumer_fence)`；consumer `max_by_key(fence)` 只采样最新 buffer 且只释放这一个。producer 跑快时前 3 帧填满 3 个 buffer，第 4 帧等 buffer0 释放，而 consumer 永不释放 buffer0/1 → producer 卡死、画面冻结在旧帧。
- 修 producer（neon-wgpu-runtime/src/lib.rs `HeadlessExternalGpu::render`）：改 `min_by_key`+`Wait` 为 CPU 侧 `consumer_fence.GetCompletedValue() >= frame_sequence` 找真正空闲 buffer，无空闲则**丢帧**（`eprintln! drop frame`）而非 GPU 等死。
- 修 consumer（bevy-nui-host/src/lib.rs `neon3_external_surface_render_system`）：`pending_release`（单）→ `pending_releases`（Vec），每帧释放**所有** imported buffer（而非只释放采样那个），让 producer 真正三缓冲复用。
- 两处均编译通过；bevy-nui-host 6 tests、neon-ui-runtime 91 tests 全过。
