# 2026-08-22

## IPC 替换施工（neon-ipc async multiplex transport）

按 `docs/ipc-replacement-evaluation-2026-08-22.md` 的结论「换 IPC 实现、不换 neon-protocol 与所有权模型」落地第一阶段：

- `crates/neon-ipc/Cargo.toml`：新增 `tokio`（net/io-util/sync/time/rt/rt-multi-thread/macros）。未引入 tokio-util，framing 手动实现以保持 wire 兼容（4 字节大端长度前缀 + JSON，与既有同步 transport 完全一致）。
- `crates/neon-ipc/src/async_rpc.rs`（新增）：
  - `AsyncRpcClient`：持久连接 + request_id demux（multiplex）、per-request timeout/cancel、bounded outbound queue、orphan/in-flight 计数、Clone 共享连接。
  - `AsyncRpcServer`：accept/read 循环不阻塞在单个 handler 上；全局 semaphore 限流；`serve`（同步 handler 走 spawn_blocking）/`serve_async`（异步 handler）两个入口；panic 映射为 Failed response 而非终止服务。
  - `RpcTransport` trait（transport 无关契约，`impl Future + Send`）。
- `crates/neon-ipc/src/lib.rs`：`map_io_error` 改 pub(crate)；`TransportError` 增加 `Cancelled` 变体 + Display；`mod async_rpc` + 重导出 `AsyncRpcClient/AsyncRpcServer/RpcTransport`。**既有同步 `RpcClient/RpcServer/EventClient` 未动，全部原功能保留**。
- 测试（async_rpc 内 `#[tokio::test]`）：multiplex 64 in-flight、慢 handler 不阻塞快请求、timeout 无 pending 遗留 + orphan 计数、cancel 返回 Cancelled、双向 wire 兼容（async↔sync）、shutdown 失败所有 pending。

状态：`cargo build -p neon-ipc` 通过（0 warning）。测试尚未跑通 —— 遇 Windows 残留 `target/debug/.cargo-lock`/`.cargo-build-lock`（时间戳 8-21 18:13，无进程占用）导致 `cargo test` 报 access denied(os error 5)，已尝试删除锁文件。用户接手自行测试。

下一步（未做）：跑 `cargo test -p neon-ipc`；全 workspace 编译回归；把 async 路径接入 neon-ui-runtime/eventd/wgpu-runtime 等 service 的迁移；named pipe transport adapter。

## IPC 替换施工（续：服务端并发化切换，14:06 后）

在 neon-ipc 层加了同步桥接（tokio 完全封装在 neon-ipc 内部，调用方无需加 tokio 依赖）：
- `AsyncRpcServer::bind_blocking / bind_blocking_with / serve_blocking / serve_until_blocking`（serve_until_blocking 等价旧 `serve_until` 的 shutdown 语义，用 stop 谓词）。
- `serve_until`（async）加 stop 谓词参数，`handle_connection` 加 stop flag + should_stop；`TransportError::Cancelled` 已加。
- `BlockingRpcClient`：同步 `call`（签名同旧 `RpcClient::call`），内部持 runtime + 持久连接。

已切换的服务端（消除单线阻塞 P0）：
- `neon-wgpu-runtime` `--headless-server`（main.rs）→ `serve_until_blocking` + `Arc<Mutex<WgpuRuntime>>`。
- `neon-wgpu-runtime` `spawn_headless_external_server` → `serve_until_blocking`（handler 原已 Arc<Mutex>，删掉了闭包内 `stop.store` 副作用——serve 返回后函数末尾本就 `stop.store(true)`）。
- `neon-wgpu-runtime` `spawn_window_server` → `serve_until_blocking` + `Arc<Mutex<WgpuRuntime>>`；用 shadow 技巧（`let mut runtime = runtime.lock()` + `let proxy = &handler_proxy`）让 130 行 handler 体几乎不改，靠 `&mut MutexGuard`→`&mut WgpuRuntime` 与 `&&EventLoopProxy`→`&EventLoopProxy` 的 deref coercion。
- `neon-projectd` `serve` → `serve_until_blocking` + `Arc<Mutex<Projectd>>`。

未改（需领域层设计，非 transport 替换）：
- `neon-ui-runtime::serve_forwarder`：串行是领域语义（host forward 顺序 + active_host_forwards 状态机 + deferred queue），需自驱动 dispatcher + job 模型。
- 客户端每请求连接的高频 forward 路径：需连接池（HashMap<SocketAddr, BlockingRpcClient>）。
- `neon-eventd`：已是每连接线程并发，无需改。

关键教训：service handler 的 `&mut` 独占借用可用 `Arc<Mutex<>>` + 闭包开头 shadow MutexGuard 的办法最小化改动；闭包若需 `move`（'static）捕获 Arc，注意不要 move 掉函数后续仍要用的局部 Arc（可 clone 或删掉闭包内冗余副作用）。

## IPC 替换施工（bug 修复：服务端连不上 os error 10061）

症状：bevy-nui-host 连不上，`transport_io: 由于目标计算机积极拒绝，无法连接 (os error 10061)`，`ui_flow_submit_failed`。根因**不是协议问题**（bevy 客户端无需任何改动，wire 格式/JSON 协议未变），而是上一轮 blocking 封装的 bug：

- `AsyncRpcServer::bind_blocking` 用**临时 current_thread runtime** 做 bind，listener 注册到该 reactor；`serve_until_blocking` 又用**另一个 multi_thread runtime** 跑 serve。tokio `TcpListener` 的 accept 依赖原 reactor，原 reactor 已 drop → accept 失败 → serve 退出 → 端口释放 → 客户端连接被拒。

修复：删掉 `AsyncRpcServer::{bind_blocking,bind_blocking_with,serve_blocking,serve_until_blocking}`，新增 `BlockingRpcServer`（持 `runtime: Runtime + server: AsyncRpcServer`），`bind` 和 `serve_until` 都 `block_on` 在**同一个** runtime 上（`let Self { runtime, server } = self;` 解构后 `runtime.block_on(server.serve_until(...))`）。调用方改：`AsyncRpcServer::bind_blocking`→`BlockingRpcServer::bind`，`.serve_until_blocking(...)`→`.serve_until(...)`。已同步改 main.rs、wgpu-runtime lib.rs（headless external + window server）、projectd。

核心教训：**tokio 的 listener/connection 必须与其 serve 的 accept/poll 在同一个 runtime 上**（listener 注册到创建它的 reactor）；同步桥接层必须让 bind 和 serve 共享同一个 runtime，不能各自临时建 runtime。

另：日志里的 `Unknown vertex attribute TEXCOORD_2` 是 Bevy 渲染侧（mesh 缺 TEXCOORD_2 顶点属性），与 IPC 无关。

## 宫崎骏风格小镇 UI 改造（组件画廊案例）

把 `imgui-component-gallery.nui`（两处副本，`crates/neon-ui-runtime/tests/fixtures/ui/` 与顶层 `tests/fixtures/ui/`，需保持一致）从暗色科技风组件画廊升级为**宫崎骏风格海滨小镇 · 汤屋商店街**主题。

- 关键约束（不能破坏 demo 代码 `demo_domain.rs`）：所有硬编码 key 必须保留 —— input 名（feature_enabled/radio_selected/slider_value/drag_value/combo_choice/dropdown_choice/tabs_choice/list_choice/item_selected/scroll_position/dialog_visible/gallery_text/progress_value/asset_window/controls_enabled）、presentation node id（feature-toggle/mode-radio/exposure-slider/count-drag/mode-combo/mode-dropdown/mode-tabs/item-list/item-selectable/gallery-scroll/action-button/gallery-text）、resource（gallery-image image、world-ui-lab.preview render_surface）、data_grid source_key asset_window + columns 字符串（原样）、drag/drop/template key（compass/potion/gem + equipment/crafting/discard + 三个 item-template）、intent 名（gallery.*、inventory.*、virtual_list.*）。
- 视觉：暗绿/暗蓝 → 天空蓝 #A8D8E8、米色 #F6EBD8、木色 #C89B6B、陶土 #D98E73、灯笼黄 #F2C94C、柔和绿 #A8C8A0、海洋蓝 #6FA8C9。
- 复杂度/变换：新增 statechart `town_tabs`（machine initial beta + state alpha/gamma + 3 条 sync），用 `tabs_choice`（enum alpha|beta|gamma）驱动三个 `branch`（面包屋/杂货铺/温泉汤屋）切换；每个 branch 内 panel + heading/body + progress_bar。
- 装饰：sky-banner 天空横幅、主题化控件 value（点亮灯笼/温泉水位/柴火捆数/游览区域/货架清单/卷轴/町内告示）、拖拽物件主题化（琉璃风铃/南瓜面包/银鳞海鱼 → 风铃挂架/烤炉/海边）。

**NUI Flow statechart 语法关键点（易错）**：`machine <key> initial <state>` 会把 initial state **自动加入 states**，因此后续不能再 `state <machine> <initial_state>`（会报 "state keys must be unique within a machine"）。所以 initial beta 时，只需 `state town_tabs alpha` + `state town_tabs gamma`（beta 已由 initial 隐含）。branch 用 `in <machine>.<state>`，sync 用 `when $enum=value -> state`，`words.len()==6`（sync）+ 关键字 `when`/`->`。

进度：已完成 .nui 视觉/主题/statechart 改造，保留全部 key；未改 Rust 代码（status 标签仍由 demo 生成英文 label）。下一步可选：给 statechart 加 `transition ... motion` + `style` 做切换动画；把 `DemoInputDomain.snapshot` 的 status label 中文化。

## 修正：真正的 bevy 案例在 D:\bevy-nui-host（不是 imgui-component-gallery）

场景 = Tokyo 微缩模型（`scenes/tokyo/littlest_matrix_tokyo.glb`，有电车动画）+ 50 个小怪物 + screen/world UI 结合。UI 定义不在 Neon3 工程，而是：
- 屏幕 UI：`D:\bevy-nui-host\assets\ui\status-flow.nui`（`flow combined-ui`，主角状态卡，被 `main.rs::monster_field_flow()` include_str）。
- 世界 UI：`D:\bevy-nui-host\src\main.rs::monster_field_flow()` 动态生成 50 个 monster world panel（`p{index}` + `machine monster_state{index}` + `input health{index}/mana{index}/level{index}` + anchor `monster.m{index}`）。

已做「可爱日系风」改造（暗蓝科技风 → 温暖可爱）：
- status-flow.nui：配色 #FFF9EF/#FFD9C0/#FFB88A/#FF8A80/#BDE7F0/#FFF3E0；头像"旅"、称号"东京的旅人"、状态"元气满满"、按钮中文化；尺寸 340x224。
- main.rs monster_field_flow：怪物面板改米白 #FFF9EF + 天蓝 #7EC8E3 边框；**新增 mp{index} 蓝条**（用 `$mana{index}`）；expanded 配色 #FFD9C0/#FF8A80。
- main.rs spawn：怪物 `vars.mana` 从 0 → 随机 `mp = 20 + (i*29)%80`，让蓝条有值。

关键约束（bevy host 硬编码，不能改）：input `health/mana/level`（CharacterStatusVars 字段映射）；node `status-root`/`status-action`（lib.rs:1961）；intent `character.status.toggle/reset/open_status(.monster.m{i})`；world panel key `p{index}`（lib.rs:1963 strip_prefix('p') 解析 index → anchor `monster.m{index}`）；`motion status-hit`（monster 的 transition 引用）；`input health{index}/mana{index}/level{index}` 由 lib.rs:3100 `format!("{key}{index}")` 从 CharacterStatusVars 字段追加 index 生成。已全部保留。

未改 Rust 交互逻辑（bevy host 的 screen/monster 状态切换、语义事件回流）；只改了 .nui 视觉 + 怪物面板内容 + mana 数据源。改动无法在本机编译验证（bevy host 独立工作区），需用户在 bevy-nui-host 侧 `cargo run` 验证。




