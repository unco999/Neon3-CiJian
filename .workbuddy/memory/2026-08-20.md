# 2026-08-20 工作日志

## UI 渲染缓存优化施工（依据 plan/neon3-ui-render-cache-optimization-plan.md）

### 阶段 0：结构化计时基线（已完成，编译通过）
- `ui_renderer.rs` 新增 `UiDrawStageTimings`（refresh_plan/compose_visuals/text_layout/group_sort/buffer_upload），
  `UiWgpuRenderer` 加 `last_stage_timings` 字段 + getter；`draw()` 内用 `Instant` 采集各阶段耗时，不改变渲染行为。
- `lib.rs` 新增 `ExternalFrameTiming`（snapshot/invalidate_plan/refresh_plan/compose_visuals/text_layout/
  group_sort/buffer_upload/color_pass/external_depth_pass/hit_pass/dropped/skipped_static/skipped_throttled），
  `HeadlessExternalGpu` 加 `frame_timings: VecDeque`（容量 256）+ `push_frame_timing` + `diagnostics()`。
- 新增 RPC `render.diagnostics` 查询最近帧计时（可按 frame_sequence 追溯，能区分「静态跳过」与「无空闲 buffer 掉帧」）。
- `render()` 签名加 `snapshot_ms` 参数（在 render 线程计时 `fragments_snapshot()` 后传入）。

### 阶段 1 核查结论（已实现，无需改动）
- 静态 snapshot dirty cache 已存在：`HeadlessExternalGpu::render` 里 `last_rendered_fragments` 相等性检查跳过整帧；
  三缓冲无空闲 buffer 时 `last_rendered_all_surfaces=false` 不清 dirty。符合文档阶段 1 验收。

### 关键代码位置（后续阶段 2+ 施工要用）
- 渲染循环入口：`lib.rs` `HeadlessExternalGpu::render()`（约 2425 行），独立渲染线程每 16ms 调 `gpu.render(&fragments, snapshot_ms)`。
- `UiWgpuRenderer::draw()`（`ui_renderer.rs` 约 3610 行）：每帧仍全量重建 instances、逐节点 `layout_text`、group 重排序、多次 `queue.write_buffer`。
- `filter_world_panels()`（`lib.rs` 约 6807 行）：每帧 clone fragment + 投影 world panel 并**写回 bounds**（阶段 2 要禁止，改为 renderer-local `UiWorldPresentation`）。
- 阶段 2 目标：camera 移动不触发字体测量/Flow compile，只更新 rect/depth。

## ⚠️ git 事故与恢复（重要）
- `git stash` + `cargo test` 期间对象数据库损坏：`.git/refs/` 目录与 `.pack` 文件丢失、`.git/worktrees/` 清空，
  git 报 "not a git repository"。工作区文件始终完好。
- 恢复：从 `.git/logs/refs/heads/*` 读 reflog 得到 master=`d2ba851`、plan=`52f4d88`，重建 refs 目录 → `git fetch origin` 恢复 objects。
- plan 分支（worktree `D:/Neon3-plan`，commit `52f4d88` 本地未推送）object 无法恢复；用户已确认不需要 plan，已删除该 worktree。
- 教训：**多 worktree 并发场景下避免 `git stash`**，改用显式 commit 或独立目录备份。

## 发现的预先存在技术债（待修）
- `cargo test -p neon-wgpu-runtime --lib` 编译失败（~42 处）：`UiVisual`/`PlannedNode`/`UiNode` 新增了
  `paint_group_id`、`world_depth` 字段后，`#[cfg(test)]` 测试代码未同步（E0062 重复字段 / E0063 缺字段）。
  分布在 `ui_renderer.rs` 测试模块（约 7642-12685 行）与 `lib.rs` 测试模块（约 9136-9700 行）。
  修复方式：给测试里的结构体字面量补 `paint_group_id: 0, world_depth: None`，或删重复字段。
