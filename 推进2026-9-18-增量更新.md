一定要灵活的运用更新策略 效率最大化 GPU数据合并写入 偏移写入等众多技巧

结论：不能把增量更新简单等同于 visible $input。

visible 只能解决：

同一棵 UI 树中，某个节点显示/隐藏
它不能正式解决：

Chat 和 Settings 的状态隔离
动态消息列表
工具调用卡片增删
会话切换
设置字段更新
变更列表更新
文件树变化
Agent 队列变化
节点结构增删
大文本 transcript 更新
revision 冲突和恢复
UI 与 Agent 状态的一致性
所以正式方案应该是：

持久化 UI Shell
  +
版本化 UI 状态模型
  +
结构化 UI Patch
  +
局部节点更新
  +
Renderer 局部提交
而不是：

build_flow()
  -> full Flow string
  -> full parse
  -> full compile
  -> full fragment replacement
正式架构

建议把右侧 Agents 页面拆成四层。

1. Static Shell
   永远存在的 UI 骨架

2. View State
   Chat / Settings / Changes / Approvals 当前状态

3. Dynamic Collections
   消息、工具调用、变更项、审批项

4. Renderer Presentation
   当前正在播放的 shader、transition、focus、selection
结构上类似：

AgentsRoot
├── Header
│   ├── AgentName
│   ├── SessionPicker
│   ├── NewSession
│   └── SettingsButton
├── ViewHost
│   ├── ChatView
│   │   ├── ContextBar
│   │   ├── Transcript
│   │   ├── TaskStatus
│   │   └── Composer
│   ├── SettingsView
│   ├── ChangesView
│   └── ApprovalsView
└── VisualOperationLayer
这个结构初次提交后长期存在。之后只更新：

Header 状态
ViewHost 当前 view
Transcript children
ToolCall children
ChangeSet children
Approval children
VisualOperationLayer
增量更新应该是什么

应定义正式的版本化 Patch 协议：

pub struct UiPatch {
    pub surface_id: String,
    pub base_revision: u64,
    pub operations: Vec<UiPatchOp>,
}
操作类型：

pub enum UiPatchOp {
    SetProperty {
        node_path: String,
        property: String,
        value: Value,
    },

    InsertNode {
        parent_path: String,
        index: usize,
        node: UiNode,
    },

    RemoveNode {
        node_path: String,
    },

    ReplaceChildren {
        parent_path: String,
        children: Vec<UiNode>,
    },

    MoveNode {
        node_path: String,
        parent_path: String,
        index: usize,
    },

    StartTransition {
        node_path: String,
        transition: TransitionSpec,
    },

    SetInput {
        key: String,
        value: UiInputValue,
    },
}
其中：

SetInput
适合高频数据变化。

SetProperty
适合节点状态更新。

InsertNode / RemoveNode
适合消息、工具卡片、审批卡片、变更卡片。

ReplaceChildren
适合一次性刷新 transcript 窗口，但不应该刷新整个 Agents 面板。

Chat/Settings 切换怎么做

正式方案不是只靠 visible，而是：

Header 点击 Setup
  -> Agent UI State: active_view = settings
  -> 生成一个 view patch
  -> 更新 ViewHost 当前 presentation
  -> ChatView 保留内存状态
  -> SettingsView 保留表单草稿
  -> Renderer 局部提交
如果当前内核第一阶段只支持 visible，可以暂时用：

SetInput("active_view", "settings")
由 runtime 的 view state machine 控制 Chat/Settings 的 presentation。

但协议层要抽象成：

active_view = chat | settings | changes | approvals
不能让业务层到处直接写：

chat_visible = false
settings_visible = true
因为那样以后无法扩展嵌套路由、动画和恢复。

最关键的区别

不正式的方案：

visible $settings
visible $chat
正式的方案：

AgentWorkbenchState {
    active_view,
    session_id,
    pending_turn,
    messages,
    tool_calls,
    approvals,
    changes,
    settings,
    visual_queue,
}
然后 UI runtime 根据这个状态计算局部 presentation。

AgentWorkbenchState revision 42
  -> UiPatch base_revision 41
  -> UiRuntime revision 42
  -> WGPU fragment delta
visible 只是最终渲染属性之一，不是整个增量系统。

状态和布局的分工

建议明确三种变化：

变化	处理方式
文本、状态、输入值	SetInput / SetProperty
消息、工具卡片、审批、变更列表	InsertNode / RemoveNode / ReplaceChildren
页面结构变化	Patch IR tree
Chat/Settings 路由	View state transition
shader、focus、scroll	Visual operation command
首次启动/严重 revision 冲突	Full mount fallback
这样 Agent 的一个操作会变成：

Agent event
  -> State reducer
  -> UiPatch
  -> Editor/Change patch
  -> VisualOperation
  -> Renderer patch
而不是 Agent 事件直接触发 build_flow()。

性能目标

正式环境应该定义预算：

Chat / Settings 切换：
  目标 < 50ms

消息文本增量：
  目标 < 16ms 到 UI state
  目标 < 100ms 到屏幕

工具卡片新增：
  目标 < 50ms

单文件操作视觉反馈：
  目标 < 100ms 开始播放

完整 Flow mount：
  只允许用于初次加载、恢复、revision 冲突
现在 638ms 的根因，是每次页面切换都做了完整 Flow 解析和重编译。即使把轮询优化到 1ms，核心成本依然存在。

推荐落地顺序

第一阶段：稳定 Shell

首次 full mount
  -> AgentsRoot 固定存在
  -> ChatView / SettingsView / ChangesView / ApprovalsView 固定节点
  -> 建立稳定 semantic node path
第二阶段：建立状态 reducer

ChatState
  -> AgentWorkbenchState
  -> reduce(AgentEvent)
  -> UiPatch
第三阶段：实现局部 Patch

ui.flow.patch
  -> revision check
  -> patch IR
  -> compile changed subtree
  -> submit fragment delta
第四阶段：动态集合

Transcript
ToolCalls
Changes
Approvals
都改成局部 children 更新。

第五阶段：性能和恢复

patch conflict
  -> 获取当前 revision
  -> 重新生成局部 patch

无法恢复
  -> full mount
最终结论

你的判断是对的：

只依靠 visible 不够正式。

visible 可以作为第一种局部属性更新，但不能作为增量 UI 的总体方案。

我们真正需要的是：

持久化 UI Shell
+
版本化 Workbench State
+
Reducer
+
结构化 UiPatch
+
局部 Fragment 更新
+
Full Mount Fallback
当前 IDE 侧的正确下一步不是继续堆 Settings 字段，而是先把 ide_shell.rs 中的：

build_flow(...)
从“每次状态变化重新生成整棵 Flow”改成：

initial_build_flow()
reduce_agent_state()
build_agent_patch()
