# neon-ui-schema

Declarative UI schema, input frames, and fragment types for Neon3.

Defines the portable UI IR, `UiFragment`, `UiProgram`, `UiInputFrame`, and
semantic event types shared between the headless UI runtime and external hosts.
It contains no GPU or window code.

## License

MIT OR Apache-2.0
