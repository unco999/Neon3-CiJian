[package]
name = "neon-ui-schema"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Declarative UI schema, input frames, and fragment types for Neon3"
repository = "https://github.com/unco999/Neon3-CiJian"
readme = "README.md"
keywords = ["neon3", "ui", "schema", "game-engine"]

[dependencies]
neon-protocol = { version = "0.1.0", path = "../neon-protocol" }
neon-world-bridge = { version = "0.1.0", path = "../neon-world-bridge" }
serde.workspace = true
serde_json.workspace = true
