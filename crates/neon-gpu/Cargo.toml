[package]
name = "neon-gpu"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Neon3 GPU compute system library: data pool addressing, layout, and compact bindings"

[dependencies]
wgpu.workspace = true
wgpu-hal = "30"
thiserror = "2"

[dev-dependencies]
pollster.workspace = true
bytemuck.workspace = true