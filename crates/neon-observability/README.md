# neon-observability

Command journal, receipts, and debug snapshots for Neon3 services.

Provides the structured command journal, `CommandReceipt`, and `DebugSnapshot`
used by Neon3 runtimes to expose auditable command history and diagnostics over
the public protocol.

## License

MIT OR Apache-2.0
