[package]
name = "neon-ui-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Headless UI declaration runtime for Neon3: NUI Flow compiler, host adapter, and input state"
repository = "https://github.com/unco999/Neon3-CiJian"
readme = "README.md"
keywords = ["neon3", "ui", "declarative", "game-engine"]

[dependencies]
neon-protocol = { version = "0.2.0", path = "../neon-protocol" }
neon-ipc = { version = "0.2.0", path = "../neon-ipc" }
neon-observability = { version = "0.2.0", path = "../neon-observability" }
neon-ui-schema = { version = "0.2.0", path = "../neon-ui-schema" }
neon-world-bridge = { version = "0.2.0", path = "../neon-world-bridge" }
serde_json.workspace = true
serde.workspace = true
png = "0.18"
