[package]
name = "neon-ui"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Unified windowless UI layer for Neon3: schema, flow compiler, and runtime entry"
repository = "https://github.com/unco999/Neon3-CiJian"
readme = "README.md"
keywords = ["neon3", "ui", "game-engine", "declarative"]

[dependencies]
neon-ui-schema = { version = "0.2.0", path = "../neon-ui-schema" }
neon-ui-runtime = { version = "0.2.0", path = "../neon-ui-runtime" }
neon-ipc = { version = "0.2.0", path = "../neon-ipc" }
